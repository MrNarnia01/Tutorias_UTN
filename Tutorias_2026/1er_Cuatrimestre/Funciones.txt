/*
    Ejemplo de funciones
*/

#include <iostream>
using namespace std;

int v[50];

void er();
int bus(int n,int ab);
int main(){
    
    for (int i = 0; i < 50; i++) {
        v[i]=0;
    }
    
    int n=0;
    do{
        int h;
        cout<<"Ingrese la cant de asociados\n";
        cin>>n;
    if(n<0 || n>50) er();
    }while(n<0 || n>50);

    for (int i = 0; i < n; i++) {
        cin>>v[i];
    }
    
    int b=0;
    cout<<"\nAsociado a buscar\n";
    cin>>b;
    
    b=bus(n,b);
    
    if(b==-1){
        cout<<"\nNo encontrado";
    }else{
        cout<<"Asociado: "<<v[b]<<"\tEn la posicion: "<<b+1;
    }
    return 0;
}

void er(){
    cout<<"\nError\n";
}

int bus(int n,int ab){
    int ia=-1;
    
    for (int i = 0; i < n; i++) {
        if(v[i]==ab) ia=i;
    }
    
    return ia;
}




